def count_character(text, target):
    count = 0
    for character in text:
        if character == target:
            count += 1
    return count
print(count_character("hello123", "l"))
print(count_character("hello there", "e"))
print(count_character("12376522", "2"))

